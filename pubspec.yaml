name: dartz_test
description: Provides matchers to help unit-testing your apps when using functional programming types defined in the dartz package.
repository: https://github.com/SuperMuel/dartz_test

version: 0.1.1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  dartz: 0.10.1
  test: 1.25.15


dev_dependencies:
  lints: ^6.0.0
